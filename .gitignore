# Audit logs. Regenerated by `python run.py compare` — 165MB of them for the
# headline run, and they are outputs rather than sources. `run.py verify` re-reads
# whatever is present, so a fresh clone verifies its own run, not mine.
data/
artifacts/

__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

.venv/
venv/
env/

.vscode/
.idea/
.DS_Store
Thumbs.db
